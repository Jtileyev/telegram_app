# Критические задачи (исправить немедленно)

## 🔴 1. Хардкоженные пути в start.sh/stop.sh
**Приоритет**: Критический
**Статус**: ❌ Не исправлено
**Файлы**: `start.sh`, `stop.sh`

### Проблема
```bash
PROJECT_DIR="/home/jaras/vscode_projects/telegram_app"
```
Пути захардкожены для конкретной системы. Скрипты не будут работать на других машинах.

### Решение
- Использовать `$(dirname "$0")` для определения директории проекта
- Сделать скрипты портируемыми

### Оценка времени
30 минут

---

## 🔴 2. Токен бота в базе данных
**Приоритет**: Критический (безопасность)
**Статус**: ❌ Не исправлено
**Файлы**: `bot/config.py`, `bot/main.py`, `init_database.py`

### Проблема
- Токен бота хранится в базе данных без шифрования
- Должен использовать переменные окружения (.env)
- Риск компрометации при утечке базы данных

### Решение
1. Создать `.env.example` файл
2. Добавить поддержку python-dotenv
3. Переписать bot/config.py для чтения из .env
4. Обновить документацию SETUP.md

### Оценка времени
1 час

---

## 🔴 3. Прямые SQL запросы в хендлерах
**Приоритет**: Критический (архитектура)
**Статус**: ❌ Не исправлено
**Файлы**: `bot/main.py` (строки 1061-1068, 1142-1159)

### Проблема
```python
# Line 1061-1068
cursor = conn.cursor()
cursor.execute("SELECT * FROM favorites WHERE user_id = ?", (user_id,))
```
Нарушение архитектуры - обход database.py абстракции.

### Решение
- Переместить SQL запросы в database.py
- Создать методы:
  - `get_user_favorites(user_id)`
  - `update_review_helpful_count(review_id, increment)`
  - `check_user_voted(user_id, review_id)`

### Оценка времени
1.5 часа

---

## 🔴 4. Дефолтные учетные данные администратора
**Приоритет**: Критический (безопасность)
**Статус**: ❌ Не исправлено
**Файлы**: `init_database.py`, `SETUP.md`

### Проблема
```
Email: atks0513@gmail.com
Password: auto-generated or 'admin'
```
Учетные данные задокументированы и видны в коде.

### Решение
1. Генерировать случайный пароль при первом запуске
2. Выводить пароль только один раз в консоль
3. Требовать смену пароля при первом входе
4. Удалить дефолтные учетные данные из документации

### Оценка времени
2 часа
